apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sRestrictedRegistries
metadata:
  name: restricted-registries
  annotations:
    # Constraints must apply *after* their ConstraintTemplate has been
    # registered as a dynamic CRD by Gatekeeper. Templates land at the
    # default sync-wave (0); these Constraints sit at wave 1.
    argocd.argoproj.io/sync-wave: "1"
spec:
  enforcementAction: deny
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["Pod"]
    excludedNamespaces:
      - kube-system
      - kube-public
      - kube-node-lease
      - gatekeeper-system
  parameters:
    # Replace these prefixes with your environment's approved registries.
    # The defaults below cover the in-tree Helm charts shipped by this repo
    # plus GKE-managed system images.
    approvedRegistries:
      - "europe-west2-docker.pkg.dev/"   # Artifact Registry, regional
      - "gcr.io/gke-release/"
      - "registry.k8s.io/"
      - "k8s.gcr.io/"
      - "quay.io/jetstack/"
      - "quay.io/prometheus/"
      - "quay.io/prometheus-operator/"
      - "grafana/"
      - "docker.io/grafana/"
      - "docker.io/nginxinc/nginx-unprivileged"
