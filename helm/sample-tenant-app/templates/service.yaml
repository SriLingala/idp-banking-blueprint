apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-tenant-app.fullname" . }}
  labels:
    {{- include "sample-tenant-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-tenant-app.selectorLabels" . | nindent 4 }}
